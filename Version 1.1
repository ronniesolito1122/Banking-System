import time

AccountArea = []
PasswordArea = []
name_map = {}
balance =+ 0
def funccc():
    print("---------------")
    functions()
def functions():
    while True:
        print("Welcome to J.R.E Banking System \nPlease select your functions below: \n O.Create Account \n 1.Log-in \n 2.Admin \n 3.Exit ")
        sel = input("Response")
        if sel == "0":
            createAccount()
        if sel == "1":
            login()
        if sel == "2":
            admin()
            storageArea1()
            storageArea2()
            match()

        if sel == "3":
            break
def createAccount():
    while True:
        CA = input("Please do enter your unique desired account number: ")

        if CA.isalpha():
            print("Invalid, enter number only")
            continue
        if len(CA) == 8:
            AccountArea.append(CA)
            break
        if len(CA) != 8:
            print("Enter exact 8 digits")
            continue

    while True:
        pin = input("Please enter your 4 digit PIN: ")
        if pin.isalpha():
            print("Invalid, enter number only")
            continue
        if len(pin) == 4:
           break
        if len(pin) != 4:
           print("4 Digits only!")
           continue

    while True:
            reEnter = input("Please RE-ENTER your PIN: ")
            PasswordArea.append(reEnter)
            if pin == reEnter:
                print("Verifying...")
                time.sleep(2)
                print("DO NOT PRESS 1, VISA FUNCTIONALITY ON PROGRESS")
                would = input("You have successfully created your account... \n Would you like to order a VISA Crad \n 1. Yes \n 2. No ")
                if would == "1":
                    VISA()

                if would == "2":
                    functions()
            if pin != reEnter:
                continue
    name_map[CA] = pin
def login():
    user_Account = input("Enter your account number: ")
    if user_Account in AccountArea:
        user_Number = input("Enter you account number: ")
        dashboard()

        if user_Account in PasswordArea:
            print("You have successfully logged in!")

    else:
        print(f"Account {user_Account} does not exist. Please reenter a valid account number.")


def storageArea1():
    print("ACCOUNTS: \n")
    for itemss in AccountArea:
        print(itemss)

def storageArea2():
    print("PASSWORDS: \n")
    for items in PasswordArea:
        print(items)


def match():
    print("Matchings: ")
    for account, password in name_map.items():
        print(f"Account: {account}, Password: {password}")
        #for updated
    if len(AccountArea) == len(PasswordArea):
        for account, password in zip(AccountArea, PasswordArea):
            print(f"Account: {account}, Password: {password}")
    else:
        print("Error: AccountArea and PasswordArea lists have different lengths.")

def dashboard():
    while True:
     print("Welcome to your account!")
     user = input("Select your function below: \n 0. Deposit\n 1. Withdraw\n 2. Check Balance\n 3. Transfer \n 4. Transaction History \n 5. Utility Bills \n 6. Order VISA \n 7. Log Out \n Response: ")

    if user == "1":
     deposit()
    if user == "2":
     withdraw()
    if user == "3":
     bankTransfer()
    if user == "4":
     transachis()


    if user == "5":
        while True:
         print("----You are about to pay your utility bill----")
         utility = input("Select electricity provider below: \n 1. Meralco \n 2. Batelec 1 \n 3. Batelec 2")
         if utility == "1":
          ElecAccNum = input("Enter your electric account number: ")
          ElecAmt = input("How much are you going to pay: ")
          print("Verifying...")
          time.sleep(2)
          print("You have successfully payed" +ElecAmt+ "to your Account number: " +ElecAccNum+ "")
         if utility == "2":
          ElecAccNum = input("Enter your electric account number: ")
          ElecAmt = input("How much are you going to pay: ")
          print("Verifying...")
          time.sleep(2)
          print("You have successfully payed" +ElecAmt+ "to your Account number: " +ElecAccNum+ "")
         if utility == "3":
          ElecAccNum = input("Enter your electric account number: ")
          ElecAmt = input("How much are you going to pay: ")
          print("Verifying...")
          time.sleep(2)
          print("You have successfully payed" +ElecAmt+ "to your Account number: " +ElecAccNum+ "")
          continue

         if user == "6":
          VISA()

    if user == "7":
         functions()

enter = input("Hi! Please press enter to proceed")
if enter == "":
    print("")
    funccc()


